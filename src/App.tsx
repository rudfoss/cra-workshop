import React from "react"
import Incrementor from "features/Incrementor"

const App: React.FC = () => {
	return (
		<div>
			<h1>React workshop</h1>
			<Incrementor />
		</div>
	)
}

export default App
